# 지금 어디까지 왔나

> **마지막 갱신** 2026-09-16 · `weakness-label` `11cbdb0`
> 세션을 끝낼 때 이 파일의 해당 칸을 고친다. 고치는 방법은 맨 아래.

## 한 줄

**사진이 노트를 남기는 데까지 왔다. 그 노트가 복습으로 이어지는 고리는 아직 안 닫혔다.**

## 목표 (🔒 2026.09.15 · 결정 B)

다시다는 **복습 앱**이다. 홈은 "오늘 복습할 것"이고, **복습 재료가 빌 때마다**(최초 1회가 아니라) 사진 등록을 요구한다. 졸업은 없다 — 재료가 떨어지면 다시 채우는 순환이다.

## 칸

칸은 **글자**로 센다. 옛 기록의 ①②③④는 방마다 다른 걸 가리켰다 — 숫자는 이제 안 쓴다.

| 칸 | 상태 | 한 줄 |
|---|---|---|
| **A** 사진이 노트를 남긴다 | ✅ | 찍으면 분석하고, 노트를 기기에 계정별로 저장한다. 사진 파일은 문서 폴더에 남는다 |
| **B** 진단을 걷어낸다 | ✅ | 10문제 화면·훅·문제은행 삭제. 학생이 들어가던 문 5개를 사진으로 돌렸다 |
| **C** 약점 이름표가 붙는다 | 🚧 **막힘** | 186칸 중 55칸만 참(코드로 다시 셌다). 빈 칸 131개 말고 **틀린 칸**이 따로 있다 — 미분에서 막힌 학생이 이차함수 처방을 받고 있었다. 미분 문구 3줄은 고쳤고 나머지 30개 풀이법은 안 봤다 |
| **D** 복습 과제가 유지된다 | ❓ **확인 안 됨** | "복습 과제가 다음 날 사라진다"는 제보. 날짜 형식이 서버와 어긋난다는데 아무도 재현 안 함 |
| **E** 노트가 복습 과제가 된다 | ⬜ | 사진 노트 → 복습 과제. **C가 먼저 풀려야 한다** (약점 없는 노트는 과제가 안 된다) |
| **F** 홈이 복습 리스트가 된다 | 🔜 **다음** | 여정보드 4노드를 걷어내고 복습 리스트로. 재료 0이면 사진을 요구한다. **C·E에 안 막힌다** — 복습이 0건이어도 "사진 찍어"가 뜨는 게 B의 설계다 |
| **G** 서버에도 저장된다 | ⬜ | 지금 노트는 기기에만 있다. 앱을 지우면 사라진다 |
| **H** 말투 통일 | ⬜ | 사진 흐름만 반말, 나머지는 해요체. B 이후 남은 게 약 79줄 |

**C · D · F는 서로 독립이라 같이 가도 된다.** 파일이 안 겹친다 (단 D는 읽기 전용일 때).
**E만 C 뒤다** — 약점 없는 노트는 복습 과제가 안 된다.

셋 중 **F가 제일 크다.** 여정보드를 쓰는 파일이 비테스트 12개 + 테스트 4개다.
그리고 F는 지우는 일이 아니라 **다시 짜는 일**이다 — 보드를 빼면 홈에 뭘 띄울지 기윤이 정해야 한다.

## C칸 — 지금 돌리는 중 (2026.09.16)

**수열 6칸.** 에이전트로 약점 초안을 만들고, 반박으로 깨고, 확인 문제를 만든 뒤
**정답을 가린 채 다른 에이전트가 풀어 대조**한다. 만드는 쪽과 채점하는 쪽을 나눈다.

1차(09.15): 6칸 전부 "그 실수는 실재한다"고 나왔는데 반박에서 5칸이 깨졌다.
깨진 이유가 중요하다 — **"수열엔 그런 실수 없다"로 깨진 건 하나도 없다.**
전부 "칸은 맞는데 써온 문장이 거짓"이었다. 통과한 1칸은 `Sₙ에서 aₙ 구할 때 n=1을 따로 안 봄`(개념 구멍).

그래서 설계를 고쳤다: **깨지면 버리지 말고 반박을 들고 다시 쓴다(최대 2회).**
2차는 고쳐쓰기 단계에 닿기 전에 죽었다. 얻은 것 없음.

**내일 이어가는 법** — 워크플로우를 `resumeFromRunId`로 재실행한다.
- 스크립트: `~/.claude/projects/-Users-baggiyun-dev-dasida-app--claude-worktrees-weakness-label/<세션>/workflows/scripts/sequence-weakness-fill-wf_6559ee28-bb3.js`
- runId: `wf_6559ee28-bb3`
- 제안 6개·반박 6개는 캐시에서 나온다(먹었는지는 확인 안 됨). 새로 도는 건 고쳐쓰기부터.

수열이 끝나면 그 절차를 그대로 **루틴**으로 굳힌다. 코드는 자동으로 안 고친다 —
재료를 파일로 쌓는 데까지만 하고, 넣을지는 기윤이 정한다. 9번 돌면 멈춘다.

## A·B에서 안 한 것 (의도적)

- `home-journey-state.ts`의 문구 — 홈에 아직 `STEP 1: 10문제 빠른 진단`이 떠 있다. 누르면 사진으로 가지만 **글자는 남아 있다.** F에서 통째로 다시 짠다 (2026.09.15에 "저장 방과 겹쳐서" 미뤘는데, 그쪽 작업이 끝나 그 이유는 사라졌다)
- `constants/diagnosis-theme.ts` · `diagnosisRouterUrl` · `diagnoseMethod` — 이름은 diagnosis인데 실모·사진이 쓴다. 지우면 모의고사가 죽는다
- `LearningSource`의 `'diagnostic'` — 빼면 기존 학생의 서버 저장이 전부 500으로 죽는다

## 지금 열린 방

| 방 | 칸 | 무엇 |
|---|---|---|
| 약점 | **C** | 세는 건 끝났다. 목표는 **빈 42칸**(기출 상위 10개 = 88.5%). 131칸 전부는 안 한다 — 나머지는 기출에 거의 안 나온다. 풀이법 하나씩 9번으로 나눈다. 지금 **수열**(2위, 11.7%) 차례 |
| 복습날짜 | **D** | 코드로 판정만. 고치지 않는다 |
| 홈 | **F** | 여정보드를 걷어내고 복습 리스트로 |

각 방은 **worktree를 따로 판다.** 같은 폴더면 서로 덮어쓴다 (2026.09.15 실측).

## 배포 전 반드시

- [x] ~~사진이 임시 폴더에 저장되던 것~~ — 고침 (`e1f772f`). 안 고치고 냈으면 학생 사진이 며칠 뒤 사라졌다
- [ ] **F를 안 하고 내면 안 된다** — 홈에 "10문제 빠른 진단"이라고 적힌 채 나간다

## 모르는 것

- 옛 ⑤가 무엇이었는지 모른다. 저장 방이 ①②④와 ⑥⑦⑧만 말했다
- 실제 학생이 몇 명이나 쓰는지 모른다. Firestore를 조회한 적이 없다
- C의 "55칸"은 코드에서 센 숫자다. 실제 학생 사진에서 몇 %가 붙는지는 안 쟀다
- **학생이 몇 명인지 모른다.** 기윤은 "매우 적다"고 확신한다(09.15). Firestore·Analytics 조회는 여전히 안 했다.
  `photo_weakness_labeled` 이벤트가 이미 심겨 있는데(`use-photo-flow.ts:459`) 아무도 안 읽었다
- "정답"이라고 박힌 **590개**(복습 생각단계 177 · 확인문제 59 · 보충흐름 354)가 맞는지 아무도 안 봤다.
  학생이 적으니 지금은 안 급하다 — 배포 직전에 꺼낸다
- 약점 하나는 세 군데가 같은 말을 해야 한다 — 노트 문구(`diagnosisMap`) · 확인 문제(`practiceMap`) · 보충 흐름(`remedial-flows`). 미분 3개는 지금 따로 논다(문구는 미분인데 확인 문제는 아직 이차함수). 나머지 56개는 안 봤다
- 약점 이름 59개 중 13개에 "계산"이 들어 있고, 고3 7개(수열·지수로그·적분·삼각함수·극한·확률·미분 계산)는 그 풀이법의 **유일한** 약점이다. 오르비에서 학생은 "계산"이라고 이름 붙은 걸 공부로 고칠 게 아니라고 분류한다(9모 뒤 50명 중 다시 본 사람 5명). 이 이름이 실제로 덮게 만드는지는 안 쟀다

## 갱신 규칙

세션 끝에 `npm run log:commit`을 돌린 뒤, **이 파일에서 건드린 칸의 상태와 한 줄을 고친다.** 맨 위 갱신 날짜와 커밋도 같이.

- 칸을 새로 만들면 **다음 글자**를 쓴다. 숫자로 돌아가지 않는다
- 칸이 막히면 🚧로 두고 **무엇이 막는지** 한 줄로 적는다. "진행 중"이라고만 적지 않는다
- 모르는 건 "모르는 것"에 적는다. 아는 척하면 이 파일이 쓸모없어진다
