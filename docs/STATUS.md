# 지금 어디까지 왔나

> **마지막 갱신** 2026-09-15 · `worktree-review-date-verdict` (main 머지 전 — 해시는 머지 때 적는다)
> 세션을 끝낼 때 이 파일의 해당 칸을 고친다. 고치는 방법은 맨 아래.

## 한 줄

**사진이 노트를 남기는 데까지 왔다. 그 노트가 복습으로 이어지는 고리는 아직 안 닫혔다.**

## 목표 (🔒 2026.09.15 · 결정 B)

다시다는 **복습 앱**이다. 홈은 "오늘 복습할 것"이고, **복습 재료가 빌 때마다**(최초 1회가 아니라) 사진 등록을 요구한다. 졸업은 없다 — 재료가 떨어지면 다시 채우는 순환이다.

## 칸

칸은 **글자**로 센다. 옛 기록의 ①②③④는 방마다 다른 걸 가리켰다 — 숫자는 이제 안 쓴다.

| 칸 | 상태 | 한 줄 |
|---|---|---|
| **A** 사진이 노트를 남긴다 | ✅ | 찍으면 분석하고, 노트를 기기에 계정별로 저장한다. 사진 파일은 문서 폴더에 남는다 |
| **B** 진단을 걷어낸다 | ✅ | 10문제 화면·훅·문제은행 삭제. 학생이 들어가던 문 5개를 사진으로 돌렸다 |
| **C** 약점 이름표가 붙는다 | 🚧 **막힘** | 분석이 다 맞아도 `weaknessIds`가 빌 때가 있다. 통역표 186칸 중 55칸만 참 |
| **D** 복습 과제가 유지된다 | 🚧 **고쳤으나 미확인** | **어긋나고 있었다. 고쳤다. 실제 앱에서는 아직 못 봤다.**<br>어긋난 내용(2026.09.15 배포 서버 실측): 앱이 `"2026-09-16"`을 보냈고 서버가 `400 Invalid ISO datetime`으로 거절했다. 진짜 범인은 날짜가 아니라 `firebase-learning-history-api.ts:17` — 그 400을 네트워크 끊김과 같은 칸에 넣어 `remote-review-task-store.ts:86`이 성공 처리했고, 다음 `load()`가 로컬 사본을 덮었다. day30 졸업 빼고 **완료 자체가 안 남았다**(배열을 통째로 보내서 한 줄이 죽으면 다 죽는다). **로그인한 학생만**(게스트는 검사가 없다).<br>고친 것: 위 세 줄(`firebase-learning-history-api.ts`·`review-scheduler.ts`·`local-learning-history-repository.ts`) + `use-quiz-hub-screen.ts`에 `.catch` 한 줄. 654개 테스트·`tsc` 통과.<br>**남은 것: 인증된 상태로 저장이 200 나오는 걸 시뮬레이터에서 못 봤다.** 401(형식 통과)까지만 봤다 — 그건 저장 성공이 아니다. 로그인이 필요해 Claude 혼자 못 닫는다. **확인 절차는 아래 "배포 전 반드시"에 적어뒀다** |
| **E** 노트가 복습 과제가 된다 | ⬜ | 사진 노트 → 복습 과제. **C가 먼저 풀려야 한다** (약점 없는 노트는 과제가 안 된다) |
| **F** 홈이 복습 리스트가 된다 | 🔜 **다음** | 여정보드 4노드를 걷어내고 복습 리스트로. 재료 0이면 사진을 요구한다. **C·E에 안 막힌다** — 복습이 0건이어도 "사진 찍어"가 뜨는 게 B의 설계다 |
| **G** 서버에도 저장된다 | ⬜ | 지금 노트는 기기에만 있다. 앱을 지우면 사라진다 |
| **H** 말투 통일 | ⬜ | 사진 흐름만 반말, 나머지는 해요체. B 이후 남은 게 약 79줄 |

**C · D · F는 서로 독립이라 같이 가도 된다.** 파일이 안 겹친다 (단 D는 읽기 전용일 때).
**E만 C 뒤다** — 약점 없는 노트는 복습 과제가 안 된다.

셋 중 **F가 제일 크다.** 여정보드를 쓰는 파일이 비테스트 12개 + 테스트 4개다.
그리고 F는 지우는 일이 아니라 **다시 짜는 일**이다 — 보드를 빼면 홈에 뭘 띄울지 기윤이 정해야 한다.

## A·B에서 안 한 것 (의도적)

- `home-journey-state.ts`의 문구 — 홈에 아직 `STEP 1: 10문제 빠른 진단`이 떠 있다. 누르면 사진으로 가지만 **글자는 남아 있다.** F에서 통째로 다시 짠다 (2026.09.15에 "저장 방과 겹쳐서" 미뤘는데, 그쪽 작업이 끝나 그 이유는 사라졌다)
- `constants/diagnosis-theme.ts` · `diagnosisRouterUrl` · `diagnoseMethod` — 이름은 diagnosis인데 실모·사진이 쓴다. 지우면 모의고사가 죽는다
- `LearningSource`의 `'diagnostic'` — 빼면 기존 학생의 서버 저장이 전부 500으로 죽는다

## 지금 열린 방

| 방 | 칸 | 무엇 |
|---|---|---|
| 약점 | **C** | 통역표 186칸 중 빈 칸을 세고, 채울지 정한다. 기윤의 수학 판단이 계속 필요하다 |
| 복습날짜 | **D** | 판정·수정 끝(2026.09.15). **시뮬레이터에서 200 확인만 남았다** — 로그인이 필요해 기윤 손이 한 번 든다 |
| 홈 | **F** | 여정보드를 걷어내고 복습 리스트로. ⚠️ 이 방 `.env`에 `EXPO_PUBLIC_SAVE_REVIEW_TASKS_URL`이 없다 — 로그인 상태로 복습을 테스트하면 `review-task-store-router.ts:28`이 예외를 던진다 (2026.09.15 발견, D방이 알림) |

각 방은 **worktree를 따로 판다.** 같은 폴더면 서로 덮어쓴다 (2026.09.15 실측).

## 배포 전 반드시

- [x] ~~사진이 임시 폴더에 저장되던 것~~ — 고침 (`e1f772f`). 안 고치고 냈으면 학생 사진이 며칠 뒤 사라졌다
- [ ] **F를 안 하고 내면 안 된다** — 홈에 "10문제 빠른 진단"이라고 적힌 채 나간다
- [ ] **D를 실제 앱에서 확인하고 내야 한다** — 코드와 테스트로는 닫았지만 로그인한 상태로 200 나오는 걸 아직 못 봤다. 401(형식 통과)까지만 봤고 **그건 저장 성공이 아니다.** 2026.09.15 기준 홈 방이 시뮬레이터를 쓰고 있어서 멈췄다. 홈 방이 놓으면 5분이면 된다:

  **0. 먼저 `.env`부터. 이거 안 하면 확인 자체가 안 된다.** 〔기윤〕
     `.env`는 gitignore라 **워크트리에 안 따라온다.** 메인에서 복사해야 한다.
     그리고 **그 `.env`에 저장 URL이 빠져 있다** — `LIST_REVIEW_TASKS_URL`은 있는데 `SAVE_REVIEW_TASKS_URL`이 없다.
     둘 다 있어야 `create-learning-history-repository.ts:38`이 서버 저장을 켠다.
     하나라도 비면 `remoteStore`가 `null`이 되고 `review-task-store-router.ts:28`이
     로그인 사용자한테 예외를 던진다 — **요청 자체가 안 나가서 200이고 400이고 볼 수가 없다.**
     ```
     EXPO_PUBLIC_SAVE_REVIEW_TASKS_URL=https://asia-northeast3-dasida-app.cloudfunctions.net/saveReviewTasks
     ```
     EAS production엔 있다(`eas env:list`로 확인). 로컬 `.env`에만 빠져 있다. 비밀 아닌 공개 함수 주소다.
     **홈 방 `.env`도 같은 파일이라 거기도 빠져 있다** — 그 방이 로그인 상태로 복습 리스트를 테스트하면 같은 예외를 만난다.

  1. 홈 방 터미널에서 `Ctrl+C` 〔기윤〕 (8081을 물고 있다). 그다음 〔Claude〕
     `cd .claude/worktrees/review-date-verdict && ./node_modules/.bin/expo start --dev-client`
     — 빌드 아니고 번들만 새로 무는 거라 몇 초다.
     (`npm run start:dev`를 쓰려면 `app.config.js:21`이 번들 ID를 `com.dasida.app.dev`로 바꾸는데 그게 안 깔려 있다. 먼저 `npm run ios:dev`로 10~20분 빌드가 든다. 대신 앱이 분리돼 홈 방과 안 겹친다)
  2. 닉네임·학년 입력 〔Claude〕 — 2026.09.15 기준 이 시뮬레이터는 온보딩 백지 상태다
  3. **구글 로그인** 〔기윤〕 ← 기윤이 손대는 건 0·1·3 셋뿐이다.
     `EXPO_PUBLIC_GOOGLE_IOS_CLIENT_ID`가 `.env`에 있어 시뮬레이터에서 사파리 창으로 된다.
  4. 모의고사 한 번 풀어 day1 과제 만들기 〔Claude〕
     — 복습 과제가 생기는 경로가 지금 이것뿐이다(진단은 B에서 걷어냈고 사진→복습인 E는 아직이다).
     서버가 만드는 이 task는 원래 유효한 ISO다. **여기서 확인하려는 건 그다음, 앱이 만드는 값이다.**
  5. 그 복습 끝내기("기억났어요") → **앱 완전 종료** → 다시 켜기 〔Claude〕
     완전 종료가 핵심이다 — 안 끄면 화면에 남은 게 서버에 저장된 건지 메모리에만 있는 건지 구분이 안 된다.
  6. **셋 다 맞아야 닫힘이다.** 〔Claude〕
     - 끝낸 복습이 **완료로 남아 있다**
     - 다음 단계(**DAY 3**) 과제가 생겼다
     - 터미널에 `[RemoteReviewTaskStore] saveAll falling back to local mirror` 가 **안 뜬다**

  하나라도 어긋나면 D는 다시 🚧다. 그 화면이나 로그를 그대로 남길 것.

## 모르는 것

- 옛 ⑤가 무엇이었는지 모른다. 저장 방이 ①②④와 ⑥⑦⑧만 말했다
- 실제 학생이 몇 명이나 쓰는지 모른다. Firestore를 조회한 적이 없다
- C의 "55칸"은 코드에서 센 숫자다. 실제 학생 사진에서 몇 %가 붙는지는 안 쟀다
- D가 실제로 학생 데이터를 먹었는지 모른다. Firestore에 이미 날짜만 적힌 줄이 있으면 `learning-history.ts:1001`의 `.parse()`가 읽을 때 500이 난다. Firebase 로그를 못 봐서 여기서 멈췄다
- 로그인한 학생이 실제로 **몇 명인지** 모른다. D의 피해 범위가 거기에 달렸다 (게스트는 무사하다)

## 갱신 규칙

세션 끝에 `npm run log:commit`을 돌린 뒤, **이 파일에서 건드린 칸의 상태와 한 줄을 고친다.** 맨 위 갱신 날짜와 커밋도 같이.

- 칸을 새로 만들면 **다음 글자**를 쓴다. 숫자로 돌아가지 않는다
- 칸이 막히면 🚧로 두고 **무엇이 막는지** 한 줄로 적는다. "진행 중"이라고만 적지 않는다
- 모르는 건 "모르는 것"에 적는다. 아는 척하면 이 파일이 쓸모없어진다
