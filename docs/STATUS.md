# 지금 어디까지 왔나

> **마지막 갱신** 2026-09-22 · main `c551b65` · **다음 첫 일은 바로 아래 「👉 다음 세션 첫 일」**
> 09.20~21 판 기록은 노션 「수열 「a₁은 S₁로 따로 확인」 복습 콘텐츠 + 정의역 구절」에도 있다.
> **C도 main에 들어갔다** (09.20에 `weakness-label`을 main에 합치고 밀었다 — `d707d04`).
> 09.19 웹 확인 기록은 아래 「웹은 따로 돈다」.
> 세션을 끝낼 때 이 파일의 해당 칸을 고친다. 고치는 방법은 맨 아래.

## 👉 다음 세션 첫 일 (2026.09.22에 고쳐 적음)

**웹 결함 ⑤ — 「모르겠어, 왜 틀렸는지 보여줘」 버튼을 단다.** `web-proto/app.js`, 파일 1개, **40분.**

🔧 **정정 — 09.21 새벽에 적힌 "채팅 ④에 탈출 버튼"은 틀렸다.**
기윤이 09.21 밤에 화면 녹화를 다시 보고 잡았다. **학생은 ④를 통과했다** — AI가
"지수·로그로 푼 것 같아, 맞아?"라고 물었고 학생은 군말 없이 "맞아"를 눌렀다. 수식 인용도
걸고넘어지지 않았다. **막힌 건 ⑤다.** 그날 밤 Claude와 Fable이 이 틀린 줄 위에서 40분짜리를
두 번 설계했고, 원본을 다시 본 기윤이 잡았다.

⑤에서 AI가 이렇게 묻는다 — *"여기 「a⁴ = c」 쓴 부분. 여기서 틀린 것 같아. 맞아?"*
학생이 한 말은 ***"어?! 이게 틀렸어요?"*** 였다.

**틀린 자리를 아는 학생이면 오답노트가 필요 없다.** 다시다가 잡으려는 건 틀린 자리를 모르는
학생인데, 그 학생한테 "여기서 틀렸지?"를 확인받으려 하면 답할 수가 없다. [맞아]도 [아니야]도
둘 다 거짓말이 된다.

- **손댈 자리 하나** — `web-proto/app.js:595-601`의 `setActions`에 세 번째 버튼을 넣는다.
  `{ label: '모르겠어, 왜 틀렸는지 보여줘', onPress: () => showWhy(idx) }`
- **갈 곳은 이미 있다** — `showWhy`가 `app.js:604`에 있고 `cand.why`(왜 틀렸는지 문장)도 들어
  있다. **새로 만드는 게 없다. 문 하나를 더 다는 것이다.**
- 배포는 `npm run deploy:proto` (`package.json:24`)
- **크기: 40분.** 09.22에 Claude가 코드를 열어 확인했다. 09.21 밤엔 "40분이냐 2일이냐"가 안
  갈려 있었다 — ③ 분석이 틀린 이유까지 만드는지 몰랐는데, `cand.why`가 이미 있다.

⚠️ **[아니야]를 누르면** `startPointing(idx+1)`이 다음 후보를 또 "여기야? 맞아?"로 묻는다
(`app.js:600`). 세 번째 버튼은 그 되묻기를 건너뛰고 바로 `showWhy`로 보낸다.

**왜 이것부터인가** — 학생 2명이 더 온다(통화한 학생이 소개). 안 고치면 둘 다 ⑤에서 또
멈춘다. 표본 수와 무관한 구조 결함이다.

**어떻게 정했나** — astra·Fable 둘에 걸었다(`docs/how-we-decide.md`). Fable이 낸 ⑤의 최종
모양은 묻지 말고 말해주는 쪽이다 — *"이게 틀린 자리야 + 왜 틀렸는지 1~2문장"* + 버튼 셋
([아, 이거였구나] / [왜 틀린 건지 아직 모르겠어] / [나 여기 이렇게 안 썼는데]).
**09.22 최소판은 세 번째 버튼 하나만 다는 것이다.**

### 그다음 — E칸. 생각보다 작다

**「모르는 것」의 "E가 얼마나 큰지 모른다"는 이제 아니다.** Fable이 열어 쟀고 Claude가 확인했다.

- `spawnMistakeReviewTasks`가 **이미 있다** (`features/learning/review-scheduler.ts:107`)
- 복습 화면이 **이미 쓰고 있다** (`features/quiz/hooks/use-review-session-screen.ts:637`)
- 홈은 `source`를 안 본다 (`local-learning-history-repository.ts:74-87`)
- 서버 `sourceId`는 max 120이라 `photo-…` id가 통과한다 (`functions/src/learning-history.ts:257`)

**손댈 건 비테스트 3 + 테스트 1, 두 판** — `use-photo-flow.ts:566` 아래 호출 하나,
`app/photo.tsx`·`photo-flow-screen.tsx`에 store 내려주기, `photo-flow-screen.test.tsx`.
단 `primaryWeaknessId`가 있는 노트만 과제가 된다(대략 3분의 1).

🚨 **예약급 — E 여는 첫 5분에 정할 것.** `spawnMistakeReviewTasks`가 `source: 'weakness-practice'`를
박는다(`review-scheduler.ts:145`). 그 값이 Firestore 문서에 남는다. 나중에 `'photo'`로 바꾸면
**서버 enum(`functions/src/learning-history.ts:192`)과 이미 저장된 문서를 둘 다** 고쳐야 한다.
지금 정하면 공짜, 나중이면 마이그레이션이다.

**나머지(작은 숙제 셋·C칸)는 미뤄도 값이 같다.** 젓가락급이다.

## 📣 마케팅 — 09.22에 방향을 갈았다

**Fable 최종 판정: 오르비에 글을 더 쓰는 게 아니라, 학생이 올린 풀이 사진에 댓글로 답한다.**

> 근거 원문(astra·Fable 네 번의 답 전문 · 갈린 자리 · 기윤이 넣은 입력) = `docs/research/2026-09-22-marketing-astra-fable.md`

- **통로** — 오르비에서 `손풀이` `풀이 봐주세요` `어디서 틀렸`로 **풀이 사진이 있는 글**을
  찾아, 그 사진을 다시다에 돌리고 **기윤이 읽고** 댓글 3~5줄: 갈린 줄 · 이유 · 그 자리에서
  손으로 할 동작 하나. 11~14·20·27·28번대만, 킬러(21·22·30)는 안 건드린다.
  **앱 이름·링크·"써봐" 안 쓴다.** 학생이 "내 것도 봐줘" 하면 그때만 링크.
- **7일 뒤 판정선 하나** — 댓글 받은 학생 중 **다른 날에 두 번째 풀이를 보낸 학생 1명.**
  0이면 접는다. 지금 기준선 0명. **유입(먼저 온 사람 수)은 기록만 하고 판정에 안 쓴다**
  (Fable이 astra 안을 받아 자기 판정선을 버린 자리 — 복습 앱이면 한 번 온 건 숫자가 아니다).
- **1일차에 세는 것** — 풀이 사진 있는 글이 하루 몇 편 올라오나. **2편 미만이면 그날 접고**
  모교 수학 선생님 1명(대안)으로 간다. 09.15 문서에 "안 셌다"로 남아 있던 숫자다.
- **같은 날 30분** — App Store Connect · Play Console에서 **검색어로 들어온 사람 수**를 본다.
  출시(2026.06) 후 한 번도 안 쟀다. astra·Fable 둘 다 짚었다.

**왜 갈았나 (실측)** — 글 14편 10주 → 글 보고 먼저 연락한 학생 **0명**, 팔로워 증분 0.
쪽지 16통 3일 → 답장 2, 통화 1, 소개 의향 2. **학생이 나온 통로는 쪽지 하나뿐이었다.**
09.15 「문」 조사: 학생이 도구를 알게 된 경로에 **"광고 보고" 0편**, "유튜브에서 강사 찾아봄"
7편, "오르비에 물어서 댓글로 정함" 10편. **학생은 파는 사람 말은 안 듣고 가르치는 사람 말은 듣는다.**

🔒 **기윤이 09.22에 그은 선 — 모르는 사람한테 같은 글을 반복해서 보내지 않는다.**
호객으로 느껴서다. 이 선을 넘는 안(복붙 쪽지 30통 · 선생님 10명에게 같은 제안 · 방장 5명 ·
학원 5곳 · 칼럼니스트 5명)은 한 주는 해도 두 주를 안 한다. **선을 안 넘으면서 센 안을 고른다.**
⚠️ 댓글 통로가 무서운 자리는 따로 있다 — **기윤이 직접 수학 판정을 쓰고, 틀리면 공개로 틀린다.**
그래서 번호대를 11~14·20·27·28로 좁혔다. 이 선도 못 넘겠으면 대안은 **모교 수학 선생님 1명**
(한 사람한테 한 번이라 호객도 아니고 공개도 아니다).

⚠️ **돈은 둘 다 부정적이다.** astra: 비용 빼고 월 1만원 남는 사용자라면 **200명**이 필요하다.
Fable: 이 안들로 1월까지 월 200만원은 **안 나온다** — 소비자 유입만으론 근거를 못 댄다.
방향이 맞는 건 학원(B2B)뿐인데 **팔 물건(선생이 보는 화면)이 제품에 없다.**

### 지난주 3편 결과 (09.19 판정 · 저장소 대조표는 09.22에 채웠다)

| 편 | 발행 | 익일 조회 | 👍 | 💬 |
|---|---|---|---|---|
| 4편(12호) | 09.14 21:00 | 90 | 0 | 0 |
| 5편(13호) | 09.17 21:04 | 79 | 0 | 1 |
| 6편(14호) | 09.18 **23:08** | **196** | 0 | **2** |

- 🔒 **셋 다 익일 좋아요 0** → 09.17에 미리 적어둔 규칙대로 **다음 3편은 「세는 줄」을 빼고
  돌린다** (`docs/research/2026-09-17-orbi-3pyeon-control-table.md`).
- **좋아요는 안 갈렸지만 댓글은 갈렸다 — 0 / 1 / 2.** 그리고 **엔딩 질문에 답이 온 게 5·6편
  연속**이다(1~4편은 0건). 익일 좋아요는 13편 중 9편이 0이라 편끼리 구분이 안 되는 눈금이다.
- 6편 댓글 하나가 **09.20 통화에서 나온 것과 같은 자리**를 가리킨다 —
  *"거기서 무슨 발상 놓쳤는지? 그런거위주로 쓰는듯"*. 표본이 1명에서 **2명**이 됐다.

## 한 줄

**홈은 세 화면을 다 갖췄는데, 학생은 두 번째에서 멈춘다.**

```
사진 0장   → 소개 화면 (web-proto 문구)      ①  된다
사진 1장~  → "아직 복습할 게 없어요"          ②  된다 ← 여기서 멈춘다
복습 있음  → 복습 리스트                      ③  만들어뒀는데 아무도 못 본다
```

②에서 ③으로 가는 다리가 **E칸**이고, 그게 아직 없다.
**고칠 자리는 홈이 아니다** — 홈은 ③까지 다 그려놨고 테스트도 통과한다.
없는 건 노트를 복습 과제로 바꿔 주는 쪽이다.

## 목표 (🔒 2026.09.15 · 결정 B)

다시다는 **복습 앱**이다. 홈은 "오늘 복습할 것"이고, **복습 재료가 빌 때마다**(최초 1회가 아니라) 사진 등록을 요구한다. 졸업은 없다 — 재료가 떨어지면 다시 채우는 순환이다.

## 칸

칸은 **글자**로 센다. 옛 기록의 ①②③④는 방마다 다른 걸 가리켰다 — 숫자는 이제 안 쓴다.

| 칸 | 상태 | 한 줄 |
|---|---|---|
| **A** 사진이 노트를 남긴다 | ✅ | 찍으면 분석하고, 노트를 기기에 계정별로 저장한다. 사진 파일은 문서 폴더에 남는다 |
| **B** 진단을 걷어낸다 | ✅ | 10문제 화면·훅·문제은행 삭제. 학생이 들어가던 문 5개를 사진으로 돌렸다 |
| **C** 약점 이름표가 붙는다 | 🚧 **막힘** | 186칸 중 **56칸** 참(빈 칸 131→130). 09.19에 수열 ①(`g3_seq_sum_term`)을 **코드에 넣었다** — `5de97b3`, 파일 6개, tsc·테스트 622개 통과. 빈 칸 말고 **틀린 칸**이 따로 있다 — 약점 56개 중 **40개**에 문제(전수 점검 09.15). 수열 ②는 미결. 자세한 건 아래 「C칸 인수인계」<br>**09.20 — 09.19에 넣은 그 약점이 막다른 화면이었다.** `g3_seq_sum_term`은 약점 60개 중 유일하게 `review-content-map`에 없다(직접 셈). 복습을 열면 단계가 0개라 `step`이 안 잡히고 **"복습을 펼치는 중…"에서 안 끝났다**. 완료 버튼도 없어서 과제가 영원히 미완으로 남고 연체만 쌓였다. 닿는 경로는 기출 진단뿐(사진 노트는 E가 없어 아직 안 닿는다). `bce637a`에서 **안내 화면으로 빠지게 고치고, 같은 구멍을 테스트로 잡게 했다** — 기존 검사들이 전부 `steps.length === 0`에서 `continue`라 아무도 안 보던 자리다. **콘텐츠 자체는 아직 없다**(3-b, 별도 판). 만들면 테스트의 `아직_안_만든_것`에서 그 줄을 지운다<br>**09.20 밤 — 그 복습 콘텐츠를 넣었다** (`94d9d76`). `heroPrompt`+`thinkingSteps` 3단계 + 보충 흐름 노드 24개(`data/remedial-flows/g3_seq_sum_term.ts`, prefix `g3sst_`). 테스트의 `아직_안_만든_것`은 **비었다** — 약점 60개 전부 복습 단계가 있다. 0단계 E 11곳 전부 ✅, tsc(앱·functions) 통과, 테스트 **635개** 통과.<br>**학생 화면 문구 셋도 같이 고쳤다** — `diagnosisMap.ts`의 `desc`·`tip`, `practiceMap.ts`의 `hint`. **정의역 구멍 때문이다**: `Sₙ=1/[n(n+1)]`(2023학년도 9월 모평 7번)처럼 n=1 대입이 안 되는 문제에서 **옛 tip이 시키는 동작 자체가 실행 불가**였고, 옛 desc의 "상수항만큼 어긋납니다"는 **등비형에서 틀렸다**(`2ⁿ⁺¹−2`는 안 어긋나는데 −2만큼이라고 말했다 — 없는 예외를 찾게 만든다). 새 문장 셋은 n=0 대입을 아예 안 쓴다. `practiceMap.ts`의 `explanation`은 **일부러 안 건드렸다**(아래 「범위 밖 줄」).<br>**검산이 걸러낸 것 둘** — ⑴ 처방에 "n=2도 확인해라"를 붙였다가 깨졌다. 두 점을 통과하고 n=3에서 틀리는 식이 **1100건**이고 점을 k=6까지 늘려도 **1100/1210 그대로**다(라그랑주 보간으로 유일해가 항상 남는다 — **점을 늘리는 방향은 원리적으로 안 닫힌다**). ⑵ step1의 S₀=0 자기모순. 제한되는 건 항등식이 아니라 **닫힌 식을 Sₙ₋₁ 자리에 대입하는 것**이라 문장을 고쳤다 |
| **D** 복습 과제가 유지된다 | ✅ | **어긋나고 있었다. 고쳤다. 실제 앱과 Firestore에서 확인했다**(2026.09.15~16). F방은 "과제가 0건이라 재현 불가"로 적었는데, 로그인한 계정엔 6월 진단이 남긴 과제가 있어서 재현됐다.<br>어긋난 내용(2026.09.15 배포 서버 실측): 앱이 `"2026-09-16"`을 보냈고 서버가 `400 Invalid ISO datetime`으로 거절했다. 진짜 범인은 날짜가 아니라 `firebase-learning-history-api.ts:17` — 그 400을 네트워크 끊김과 같은 칸에 넣어 `remote-review-task-store.ts:86`이 성공 처리했고, 다음 `load()`가 로컬 사본을 덮었다. day30 졸업 빼고 **완료 자체가 안 남았다**(배열을 통째로 보내서 한 줄이 죽으면 다 죽는다). **로그인한 학생만**(게스트는 검사가 없다).<br>고친 것: 위 세 줄(`firebase-learning-history-api.ts`·`review-scheduler.ts`·`local-learning-history-repository.ts`) + `use-quiz-hub-screen.ts`에 `.catch` 한 줄. main(F 포함) 병합 후 **622개 테스트·`tsc` 통과**(병합 전 654개였는데 F가 여정보드 테스트를 지워 줄었다). `.catch`는 자동 병합에서 살아남은 것을 확인했다.<br>**확인 방법(2026.09.15에 확인. 구글 로그인 + 맥 시계를 9/17로 잠깐 옮겨 due를 만들었다 — 그래서 아래 날짜가 미래로 보인다)**: `applyOverduePenalties`에 임시 로그를 걸고 서버에서 `load()`한 값을 봤다 — `sf: "2026-09-18T00:00:00.000Z"`. **이 형식 자체가 증거다.** 고치기 전이면 `"2026-09-18"`이었고 400이라 서버에 남지도 못했다. `stage`도 day3→day1로 실제 강등돼 있었다.<br>**그리고 응답 코드를 직접 찍어서 봤다** — `HTTP 200 POST saveReviewTasks`. 처음엔 "예외가 안 났으니 200일 것"이라고 **추론**해놓고 확인했다고 적었는데, 기윤이 되물어서 실제로 찍어봤다.<br>**마지막으로 Firestore 원본을 콘솔에서 직접 봤다** — `users/user:iWaBuAJmxqTkpaeT0Co7KFL95o93/reviewTasks/attempt-mpxx3z8h-gq40py8f__g3_conic__day1`:<br>　`createdAt "2026-06-03T10:22:57.889Z"` ← 서버가 만든 값(`toISOString()`이라 임의 시각)<br>　`scheduledFor "2026-09-20T00:00:00.000Z"` ← **앱이 만든 값**(`addDaysToToday`라 시각이 0 고정)<br>**한 문서 안에 두 형식이 나란히 있다.** 앱이 만든 값이 Firestore에 실제로 들어가 있다는 뜻이다. `stage`도 `day1`로 강등돼 있다. **400·200·원본 셋 다 실측. 추론 없음** |
| **E** 노트가 복습 과제가 된다 | ⬜ **가장 급함** | 사진 노트 → 복습 과제. 학생이 ②에서 멈추는 직접 원인 |
| **F** 홈이 복습 리스트가 된다 | ✅ | 여정보드·하단 CTA·"학습 여정" 배너·태블릿 split 걷어냄. 홈은 4갈래 — 사진 0장이면 web-proto 소개 화면, 재료 0이면 "아직 복습할 게 없어요"+사진 카드, 오늘 차례 아니면 D-N 카드, 복습 있으면 리스트(끝에 사진 한 줄). 복습 카드에 걸려 있던 졸업 게이트도 같이 뺐다. 시뮬레이터 확인함 |
| **G** 서버에도 저장된다 | ⬜ | 지금 노트는 기기에만 있다. 앱을 지우면 사라진다 |
| **H** 말투 통일 | ⬜ | 사진 흐름만 반말, 나머지는 해요체. B 이후 남은 게 약 79줄 |

### 🔧 정정 — "E는 C 뒤다"는 절반만 맞다 (2026.09.15)

이 파일은 오래 **"E는 C가 먼저 풀려야 한다"**고 적어 왔다. F 작업 중 노트 구조를
열어보니 **절반만 맞는 말이었다.**

`features/photo/types.ts`의 `PhotoNote`에 이게 이미 들어 있다:

```
weaknessIds: WeaknessId[]             통역표가 찾은 약점들
primaryWeaknessId: WeaknessId | null  하나로 좁혀진 것
```

복습 과제를 만드는 데 필요한 건 **약점 id 하나**인데, 그게 이미 노트에 붙어 있다.

- 노트 **전부**를 과제로 만들려면 → C가 필요하다 (186칸 중 55칸만 참이니까)
- **약점이 붙은 노트만** 만들려면 → **C 없이 지금 된다.** 대략 3분의 1

**그래서 C를 기다릴 이유가 없다.** C는 "과제가 얼마나 많이 생기냐"를 정할 뿐이고,
E는 "과제가 생기냐 마냐"를 정한다. 0건에서 벗어나는 건 E다.

C는 기윤의 수학 판단이 계속 필요한 제일 느린 칸이다. 이 정정이 없으면
다음 방이 C부터 붙잡고 한참 돌아간다.

**단, E를 열기 전에 확인할 것:** `review-task-store` 쪽을 복습날짜(D) 방이
보고 있다. 겹치면 지금 열면 안 된다. (F방은 이 확인을 안 했다 — 남의 방이라 안 열었다)

---

**C · D는 서로 독립이라 같이 가도 된다.** 파일이 안 겹친다 (단 D는 읽기 전용일 때).

**순서 제안: E → D → C.**
E가 있어야 과제가 생기고, 과제가 생겨야 D를 재현할 수 있고, C는 양을 늘리는 일이라 마지막이다.

F에서 실제로 손댄 규모: 비테스트 8개 수정·삭제 + 새 파일 4개, 테스트 9개.
(2026.09.15 이전 기록의 "비테스트 12개 + 테스트 4개"는 잘못 센 숫자였다)

## C칸 인수인계 — 새 세션은 여기부터 (2026.09.16)

### 한 줄

**수열 약점 ①이 09.19에 코드에 들어갔다**(`5de97b3`). 빈 칸 131 → 130.
09.15~16 이틀은 조사였고(코드에 들어간 건 미분 문구 3줄뿐, `555b00e`), 09.19에 ① 하나가 붙었다.
**남은 건 ②(미결)와 다음 8개 풀이법.** 다음 판을 시작하기 전에 정할 게 하나 있다 —
`target-student`를 앞에 둘 것인가(Fable), 거부권을 뺄 것인가(astra). 아래 「외부 모델」 참고.

### 시작하는 법

worktree가 세션마다 사라진다. 매번 다시 판다.

```bash
cd /Users/baggiyun/dev/dasida-app
git worktree add .claude/worktrees/weakness-label weakness-label 2>/dev/null
cd .claude/worktrees/weakness-label
ln -sfn /Users/baggiyun/dev/dasida-app/node_modules node_modules
```

**테스트는 그냥 돌리면 0개가 돈다.** `jest.config`가 `.claude/worktrees`를 걸러낸다. 우회:

```bash
./node_modules/.bin/jest --testPathIgnorePatterns '/node_modules/' '/tests/' '/functions/' '/.expo/'
```

기준선 **622개 통과**. `./node_modules/.bin/tsc --noEmit`도 통과 상태.
`npx`는 rtk 훅이 가로채니 `./node_modules/.bin/` 을 쓴다.

### ✅ 끝남 — 수열 ①이 코드에 들어갔다 (2026-09-19 · `5de97b3`)

`g3_seq_sum_term` · 수열 × `concept_gap` · 이름 `a₁은 S₁로 따로 확인`.
빈 칸 **131 → 130**, 약점 **59 → 60**개. tsc(앱·functions) · 테스트 622개 통과.
확인 문제 정답은 python3로 재계산했다 — S_n=n²+4n+5에서 a₁=S₁=10, a₅=13, 합 23.

**고친 파일은 5개가 아니라 6개였다.** 이 문서가 5개라고 적어둔 게 부족했다.

| # | 파일 | 무엇 |
|---|---|---|
| 1 | `data/diagnosisMap.ts` | union · `weaknessOrder` · 본문 |
| 2 | `data/practiceMap.ts` | 확인 문제 (전체 Record라 없으면 컴파일 안 됨) |
| 3 | `data/diagnosisTree.ts` | 선택지 추가 + 기존 `seq_sum` 문구 좁힘 |
| 4 | `features/photo/flow/weakness-mistake-type-map.ts` | 태그 `concept_gap` |
| 5 | 같은 이름 `.test.ts` | 박힌 숫자 5개 (59→60 · 19→20 · 131→130 · 50→51) |
| **6** | **`functions/src/learning-history.ts`** | **`weaknessOrder`가 zod 스키마(`WeaknessIdSchema`)다. 여기 없는 id는 서버가 거부한다** — 진단 경로로 이 약점이 나오면 학습기록이 안 남을 자리였다. astra 지적으로 발견 |

**아직 없는 것 — 복습 콘텐츠.** `review-content-map`에 `heroPrompt`·`thinkingSteps`가
없고 `remedial-flows/` 파일도 없다. 지금은 `getReviewHeroPrompt`가 tip을 폴백으로 쓴다.
`weakness-removal-safety.test.ts`가 "비어있지 않다"만 봐서 이걸 **못 잡는다**.
사진→복습 연결(E칸)이 아직 없어 당장은 안 터지지만, 기출 진단으로 이 약점이 나오면 드러난다.
**다음 약점부터는 이것까지 한 세트로 본다.**

### 외부 모델 두 대에 물은 결과 (2026-09-19)

기윤 지시 🔒 — **앞으로 결정은 astra(`codex exec`, `gpt-6-astra`)와 Fable(`Agent`, `model: fable`)
둘 다에 건다.** Claude 단독 판단이 이 세션에서 두 번 틀렸다.
(codex는 `~/.codex/config.toml`의 `[mcp_servers.notion]`·`[stitch]`에 `command`가 없어 시작을
거부한다 — 임시 `CODEX_HOME`에 `auth.json`만 복사해 우회한다. CLI는 0.154.0으로 올렸다)

**둘이 같은 말을 한 것 넷:**
1. 같은 방식으로 9번 더 돌리지 마라
2. **만들기 전에 코드로 걸러라.** 깨진 4칸 중 "공식 기억"과 "마무리 해석"은 스크립트 20줄로
   에이전트 0개에 잡혔다. astra는 직접 돌려서 확인했다 — 수열에 `formula_recall`을 하나 더
   넣자 `primaryWeaknessId`가 `g3_sequence → null`로 떨어졌다
3. 묶음을 작게 (수열 6칸 → 2~3칸)
4. **"동작 4개면 안 한다"는 근거가 없다.** astra가 원문을 열어보니 분모가 학생 50명이 아니라
   **글 50편**이고, "개념부족으로 틀린 것도 아니고" 인용은 **지구과학 글**이었다.
   Fable도 `docs/research/`에서 grep 0건을 확인했다

**둘이 갈린 것 하나 — 아직 안 정했다:**
- **Fable**: 순서를 뒤집어라. `target-student`가 **이름·선택지·처방 골격을 먼저** 보고,
  그 제약 안에서 `math-teacher`가 쓴다. 고쳐쓰기는 **1회만**(2차 수정한 3칸이 전부 실패, 통과 0건)
- **astra**: 순서는 그대로 두되 `target-student`에게서 **거부권을 뺀다.** 표현 개선과 실험 가설만
  내게 하고, 탈락 권한은 실제 학생 5~8명으로 확인하기 전까지 주지 않는다

### 루틴 v2 — 다음 판은 이 설계로 (2026.09.20)

**전문은 `docs/weakness-routine-v2-2026-09-20.md`에 있다.** 다음 판을 열기 전에 그걸 읽는다.

- **배치 🔒** — 0단계 코드 → 만들기 `weakness-author`(Opus) → 검산 `math-checker`(gpt-5.6-sol `high`)
  → 린트 `target-student`(Haiku). 고쳐쓰기 1회. `xhigh` 안 씀
- **판단은 astra+Fable, 실행은 sol** (기윤 지시). 모델을 바꾸면 기윤에게 말하고,
  답을 전할 때 어느 모델이 말한 건지 같이 적는다
- **에이전트 정의 개편은 main `90fc19f`에 들어갔다** — `weakness-author` 신설,
  `math-checker` 모드 둘·갈래 일곱·판정 3단계, `target-student` 탈락 권한 제거,
  `scripts/sync-codex-agents.mjs`로 `.codex` 사본 맞추기
- **0단계 스크립트는 아직 코드가 없다.** 검사 항목 A~E는 정해졌다(v2 문서 2절).
  ⚠️ 코드가 온전히 막는 건 09.16 4건 중 **「공식 기억」 1건뿐**이다. 나머지는 경고·누락 확인까지고
  수학은 코드가 못 한다
- **수열 ②는 약점 **두 개**(곱셈/부호)로 간다** — 하나만 넣으면 부호 틀린 학생이 틀린 이름표를 받는다.
  "빈 것 = 0, 틀린 것 = 마이너스". 아직 안 만들었다
- **말풍선이 E보다 먼저다** — 후보가 둘이면 `primaryWeaknessId`가 null이라,
  E만 붙이면 그 노트들은 과제가 0건이 된다

### 다음 판 첫 일감 — step3 선택지 짝 맞추기 (2026.09.20 밤)

`data/review-content-map.ts`의 `g3_seq_sum_term.step3` **정답 선택지가 본문의 세 갈래 중 둘만 말한다.**

```
body      넣을 수 있으면 비교 / 넣을 수 없거나 다르면 a₁=S₁ / 같으면 그 식 하나
정답 선택지  다르면 a₁=S₁로 적고, n≥2는 그 식을 그대로 쓴다     ← "넣을 수 없으면"이 없다
```

**거짓은 아니다** — "다르면 S₁"은 참이고, 오답 둘은 새 본문에서도 정답이 될 수 없다.
그래서 이번엔 안 고쳤다(Fable 판정). 고칠 문장:

> `넣을 수 없거나 다르면 a₁=S₁로 적고, n≥2는 그 식을 그대로 쓴다`

⚠️ **그때 검산기에 `body`·`choices`·`example`을 한 묶음으로 준다.**
이번에 생긴 이유가 그거다 — 3차 검산 일감에 **body만 넣고 choices를 빼서** 검산기가
바뀐 짝을 본 적이 없다. Claude 실수다.

### 범위 밖 줄 — 이번 판에서 두 번 나왔다 (2026.09.20 밤)

만든 쪽(`weakness-author`)이 **지정 범위 밖 줄을 건드렸고**, 그게 검산에서 깨졌다.

- `data/practiceMap.ts:444-445` `explanation`의 `이 식은` → `2n+3은`.
  **시키지 않은 편집이었고 거짓이 됐다** — 다항식 `2n+3`은 n=1에 넣을 수 있다(5가 나온다).
  n≥2에서만 성립하는 건 `aₙ=2n+3`이라는 **등식**이다. 게다가 바로 다음 문장이 n=1을 실제로 넣는다.
- 그래서 **그 줄만 버리고 나머지를 넣었다.** `explanation`은 배포된 그대로 뒀다 —
  `이 식은`의 지시 대상을 바로 앞 등식 `a_n = S_n − S_{n−1} = 2n+3`으로 읽으면 참이다.

**규칙으로 박는다 (🔒 2026.09.20, Fable 결정):**

> **만든 쪽이 지정 범위 밖 줄을 건드리면 그 줄만 버린다. 세트는 안 버린다.**

「고쳐쓰기 1회로 안 되면 칸을 비운다」와 충돌하지 않는다 — 비우는 건 **세트가 깨졌을 때**고,
범위 밖 줄 하나가 깨진 건 세트가 깨진 게 아니다. 자세한 건 `docs/weakness-round-howto.md`.

### 아직 안 고친 것 — `explanation`의 모호한 지시어

`data/practiceMap.ts:445`의 `이 식은`은 **참으로 읽을 수도 거짓으로 읽을 수도 있다.**
지금은 참 쪽 읽기로 두고 안 건드렸다. 고치려면 `aₙ = 2n+3 은 n≥2에서만 성립합니다` 꼴로
**등식을 주어로** 만들어야 한다. 별도 판.

### 👉 다음 세션은 여기부터 (2026.09.20 · v2 첫 판 끝남)

**판 기록 전문은 `docs/weakness-fill-sequence-calc-slip-2026-09-20.md`.** 손 쓰는 법은 `docs/weakness-round-howto.md`.

#### 이번 판 결과 — 코드에 들어간 것 **0칸**

| | 09.16 (v1) | 09.20 (v2) |
|---|---|---|
| 호출 | 47회 | **8회** (astra 1회) |
| 코드 반영 | 1칸 | **0칸** |
| 틀린 처방 차단 | — | **1건** |

⚠️ 47 → 8을 "생산성 6배"로 읽으면 안 된다 — v1은 빈 5칸, 이번은 1칸이다. 대상이 다르다.

#### 부호 — **승인 · 미투입** (서랍에 있다)

`g3_seq_sum_sign_flip` 「Σ 쪼갤 때 빼기 부호가 뒤집힘」. 검산 두 번 통과(6,156쌍 · 20,100쌍 반례 0건).
**세트 전문은 판 기록 문서에 있다.**

못 넣은 이유 — 이 칸은 약점 둘로 설계했는데 곱셈이 깨졌다. 부호만 넣으면 후보가 1개라
`primaryWeaknessId`에 박히고, **곱셈 틀린 학생도 "부호 뒤집힘"을 받는다.**
⚠️ `primaryWeaknessId`만 비워도 소용없다 — 카드가 `weaknessIds`로 이름표를 그린다(`photo-note-card.tsx:65`).

#### 곱셈 — **보류.** 진짜 원인을 찾았다

`g3_seq_sum_multiply_slip`. 세 번 고쳐 세 번 다 깨졌다. 그런데 문장 문제가 아니었다.

```
두 계산을 x = A·B + e₁ , y = A·T + e₂ 라 하면
y − x = A(T − B) 가 성립하는 조건은  e₁ = e₂
```

**두 오차가 같기만 하면 통과한다.** Fable 실측 — 1,428,840쌍에서 통과 68,040건이 전부 `e₁ = e₂`.

> 이 대조는 **"두 줄이 다르게 틀렸을 때만"** 잡는다. 같은 버릇으로 두 번 틀리면 못 잡는다.
> **버릇 잡는 처방이 버릇에 제일 약한 구조다.**

`calc_slip`이 바로 그 습관성 실수다. **처방과 대상이 어긋나 있었다.**

→ 4차 고쳐쓰기 안 한다. 다음 후보는 **"부분곱을 적는 절차"**(`9×47` → `9×40`, `9×7` 따로).

#### 루틴에 고친 것

`weakness-author`·`math-checker`에 **「성립 조건 / 못 잡는 경우」** 칸을 신설했다(main).
검산기는 **그 줄부터 깬다.** 린트 단어 확장은 안 했다 — 3회 중 1회만 잡혀 값이 없다.

#### ✅ 말풍선 — 만들었다 (`d846d45`)

08.11 🔒였는데 "부를 사람이 없어서" 미뤄뒀던 것. 09.20에 만들었다.

```
"어디서 실수한 것 같아? 잘 모르겠으면 넘어가도 돼."
[ √를 간소화하거나 묶는 단계가 헷갈렸어요. ]
[ 분모 유리화 과정에서 실수했어요. ]
[ 켤레식으로 유리화하는 계산에서 실수했어요. ]
[ 잘 모르겠어 ]
```

**186칸 중 5칸에서만 뜬다.** tsc·테스트 633개 통과(622 → 633).
설계는 astra·Fable 둘에 걸고 Fable이 최종으로 정했다. 커밋 메시지에 전문이 있다.

버튼 문구는 `diagnosisTree` 선택지 문장을 쓴다(`weaknessChoiceText` 신설) —
`labelKo`는 「분모 유리화 실수」와 「유리화 계산 오류」가 같이 떠서 학생이 못 가른다.

**안 건드린 것** — `use-photo-thread.ts`의 연타 잠금. `press`가 콜백을 무조건 실행해서
동기 잠금이 없는데, 이번 작업이 만든 위험이 아니다(재도전 버튼도 같은 모양). 별도 커밋.

#### 🔑 다음 판 — 수열 곱셈/부호를 데이터에 넣기

말풍선이 생겼으니 **부호를 서랍에서 꺼낼 수 있다.** 단 곱셈이 아직 없다.

- 곱셈은 **부분곱 절차**로 새로 만든다 (판 기록 문서 참고). 어림 대조는 버린다
- 둘 다 서면 `diagnosisMap`·`weaknessMistakeType`·`diagnosisTree` **세 군데**에 넣는다.
  통역표에만 넣으면 안 뜬다 — 후보 함수가 `diagnosisTree`에 달린 약점만 읽는다

### 막힌 것 — 수열 ② (선생과 학생이 부딪혔다)

`g3_seq_sum_multiply_slip`("수열 합에서 곱셈 한 줄 미끄러짐", 수열 × `calc_slip`).
수학·검산은 통과했는데 **학생 눈에서 걸렸고, 고치라는 방향이 수학 쪽과 정반대다.**

- 학생: "십의 자리·일의 자리로 갈라 다시 적어라"로 줄여라 (동작 하나)
- 수학: **그 줄이 학생이 이미 지나간 줄**이다. 습관 슬립이면 같은 값이 또 나와 검사를 통과한다
- 학생: 그래서 만든 `6·80=480` 우회 처방은 동작이 **4개**라 "그 시간에 문제 하나 더 풀지"로 샌다

**읽히는 처방은 안 듣고, 듣는 처방은 안 읽힌다.**
이건 수열만의 문제가 아니라 남은 8개 풀이법에서 계속 나온다.
**누가 이기는지 기윤이 정해야 루틴이 굴러간다. 아직 안 정했다.**

### 범위 — 131칸 전부가 아니다

빈 칸 131개 중 **42칸**만 채운다. 기출 상위 10개 풀이법 = **88.5%**.
기출 1800문항(시험지 60개, `data/exam/`의 `diagnosisMethods`)으로 쟀다.
나머지 89칸은 학생이 거의 안 만난다 — `완전제곱식`은 1800문항 중 **0번** 나온다.

**풀이법 하나씩 9번**으로 나눈다. 미분은 절반 했고 수열이 지금 차례다.

### 수열 6칸 최종

| 칸 | 상태 |
|---|---|
| 공식 기억 | 원래 차 있었음 (`g3_sequence`). **새로 만들면 안 되는 칸이다** — 후보가 2개가 되면 `primaryWeaknessId`가 null로 떨어진다 |
| **개념 구멍** | **✅ ① 들어감 (`5de97b3`)** — 단 복습 콘텐츠는 아직 없다 |
| 계산 손실수 | ② 만들었는데 학생 눈에서 막힘. **막은 근거가 약하다는 게 09.19에 드러났다** |
| 식 세우기 · 절차 누락 · 마무리 해석 | 두 번씩 고쳐 썼는데 두 번 다 깨짐 |

**"수열엔 그런 실수 없다"로 깨진 칸은 하나도 없다.** 6칸 전부 "그 실수는 실재한다"를 통과했고,
깨진 건 전부 **써온 문장**이었다.

### 상세 기록 — 지우면 에이전트 118개를 다시 돌려야 한다

| 파일 | 뭐가 들었나 |
|---|---|
| `docs/weakness-copy-audit-2026-09-15.md` | 약점 56개 전수 점검(에이전트 71개). 반박을 버틴 **44건** — 약점별로 무엇이 틀렸나·대체 처방·근거 예제. 거짓 7 / 딴단원 2 / 마음가짐문구 1 / 범위부족 34 |
| `docs/weakness-fill-sequence-2026-09-16.md` | 수열 6칸(에이전트 47개). 통과 2칸 문구·확인문제 전문, 포기 4칸 이유, 선생·학생 충돌 |
| `docs/weakness-agent-reports-2026-09-15.md` | 단발 에이전트 원본. **미분 빈 3칸의 약점·처방 초안**(반박 미통과) · 적분/극한 공유 약점 · 미분 문구의 학생 눈 판정 |

### 루틴 규칙 (지금까지 정해진 것)

1. ~~깨지면 반박을 들고 다시 쓴다(최대 2회)~~ → **고쳐쓰기는 1회.**
   🚫 **2026.09.19 고침.** 2차 고쳐쓰기를 한 3칸이 **전부 실패**했다(통과 0건). 두 번째는 값이 없었다
2. **약점 설명에 "학생이 ~했는데"라고 단정하면 안 된다.** 규칙은 그대로인데 **이유가 틀렸다.**
   ~~사진 흐름은 학생한테 아무것도 안 묻는다~~ → 묻는다. `use-photo-flow.ts:337`이
   **"이 줄에서 틀린 게 맞아?"**를 묻고 답을 받는다. 다만 **왜 틀렸는지는 안 묻는다** —
   그래서 학생 행동 단정은 여전히 근거가 없다 (2026.09.19 정정)
3. 🚫 ~~약점 이름에 **"계산"**이 들어가면 학생이 덮는다 (50명 중 5명)~~
   **2026.09.19 무효.** 원문을 다시 열어보니 분모가 학생 50**명**이 아니라 글 50**편**이고,
   핵심 인용은 **지구과학1** 글이었다. 집계에 과목 필터도 없다.
   재료 문서 59줄이 스스로 편→명으로 잘못 적어둔 걸 그대로 물고 온 것이다(그 줄은 고쳤다).
   **가설로는 남기되 탈락 근거로 쓰지 않는다.** 쓰려면 프롬프트에 넣고, 정의 파일엔 박지 않는다
4. 처방은 **손으로 할 동작 하나**. 마음가짐 문구 금지 (커밋 `a76a9db`)
5. **코드는 자동으로 안 고친다.** 에이전트는 재료를 파일로 쌓는 데까지, 넣을지는 기윤이 정한다
6. 루틴은 **끝이 있어야 한다.** 9번 돌면 멈춘다
7. **약점 하나는 한 세트다** (2026.09.19 신설) — 이름·설명·처방·선택지·확인문제에
   **복습 콘텐츠**(`heroPrompt`·`thinkingSteps`·보충 흐름)와 **서버 허용 id**까지 한 묶음.
   09.19에 `g3_seq_sum_term`을 넣으면서 복습 콘텐츠를 빠뜨렸다

⚠️ **다음 판 프롬프트에 1번 옛 문구와 3번을 가져가지 말 것.** 둘 다 09.19에 뒤집혔는데
문서에 흔적이 남아 있어서 실수로 딸려가기 쉽다.

### 아직 안 정한 것

- **선생과 학생이 부딪히면 누가 이기나** (위 수열 ②)
- 반박이 "통과시키되 이건 고쳐라"고 한 **권고를 받는 단계가 없다.** 지금은 그냥 지나간다
- 실수유형이 왜 6개인지 근거가 문서에 없다. 약점 59개 중 위 세 유형이 50개, 아래 세 유형이 9개로
  쏠려 있는데, 그게 실제 분포인지 분류가 안 맞는 건지 모른다 (학생 데이터가 없다)

### 나중에 — 590개 검산

"정답"이라고 박힌 곳이 **590개**다 (복습 생각단계 177 · 확인문제 59 · 보충흐름 354).
**맞는지 아무도 안 봤고, 앱이 오답을 정답이라 가르치는 곳이 이미 하나 확인됐다**
(`review-content-map.ts:120` — `ax²+bx+c`에서 `b/2`를 구하라는데 a≠1이면 거짓).
학생이 적어서 지금은 안 급하다. **배포 직전에 꺼낸다.**
방법: `math-checker`에게 정답을 가린 채 풀린 뒤 코드로 대조. 확인 문제 59개부터.


## A·B에서 안 한 것 (의도적)

- ~~`home-journey-state.ts`의 문구~~ — F에서 파일째 삭제했다 (`6b630e0`)
- `constants/diagnosis-theme.ts` · `diagnosisRouterUrl` · `diagnoseMethod` — 이름은 diagnosis인데 실모·사진이 쓴다. 지우면 모의고사가 죽는다
- `LearningSource`의 `'diagnostic'` — 빼면 기존 학생의 서버 저장이 전부 500으로 죽는다
- `HomeLearningState`의 `heroTitle` · `heroBody` · `heroMeta` · `recentResultCard` · `featuredExamCard` — 계산은 되는데 **어느 화면도 안 그린다.** 안에 "빠른 재진단 10문제" 같은 옛 문구가 남아 있지만 눈에 보이진 않는다. F에서 손대지 않았다

## 지금 열린 방

| 방 | 칸 | 무엇 |
|---|---|---|
| 약점 | **C** | 통역표 186칸 중 빈 칸을 세고, 채울지 정한다. 기윤의 수학 판단이 계속 필요하다 |
| ~~복습날짜~~ | ~~**D**~~ | ✅ 끝. 판정·수정·실제 앱·Firestore 원본까지 확인(2026.09.16). main 병합 중 |
| ~~홈~~ | ~~**F**~~ | ✅ 끝. `a908158`로 main에 들어갔다 |

**다음에 열 방은 E다.** 홈이 준비를 마쳤고 복습 저장도 고쳤으니, 이제 채울 것을 만드는 쪽이다.

각 방은 **worktree를 따로 판다.** 같은 폴더면 서로 덮어쓴다 (2026.09.15 실측).

**worktree 안에서는 `npm test`가 테스트를 0개 본다.** `jest.config.js`의
`testPathIgnorePatterns`에 `/.claude/worktrees/`가 있어서 자기 자신을 걸러낸다.
돌리려면 그 항목을 뺀 목록으로 덮어써야 한다:
`./node_modules/.bin/jest --testPathIgnorePatterns='/node_modules/' --testPathIgnorePatterns='/tests/' --testPathIgnorePatterns='/functions/' --testPathIgnorePatterns='/.expo/'`
(그리고 `npx jest`는 rtk가 출력을 삼킨다 — `./node_modules/.bin/jest`를 쓴다)

## 배포 전 반드시

- [x] ~~사진이 임시 폴더에 저장되던 것~~ — 고침 (`e1f772f`). 안 고치고 냈으면 학생 사진이 며칠 뒤 사라졌다
- [x] ~~F를 안 하고 내면 안 된다~~ — 고침 (`6b630e0`). 홈에서 "10문제 빠른 진단" 글자가 사라졌다
- [x] ~~F를 시뮬레이터에서 한 번은 켜봐야 한다~~ — iPhone 17 Pro에서 확인 (`3bcf73b`). 빌드·번들 에러 0. **켜보고 나서야 홈이 복습 얘기를 한 마디도 안 한다는 걸 알았다** — 테스트는 다 통과했는데도 그랬다. 여백도 세 번 찍어보고서야 맞췄다
- [ ] **E 없이 내면 "복습 앱"인데 복습이 없다** — 학생은 사진을 찍고 노트를 받고 거기서 끝난다. 다음 날도, 그다음 날도 ②만 본다. 지금 내도 앱이 깨지진 않지만, **다시다가 파는 게 복습인데 복습이 한 번도 안 온다.** 낼지 말지는 기윤이 정한다 — 이 파일은 사실만 적는다
- [x] ~~D를 실제 앱에서 확인하고 내야 한다~~ — **2026.09.16 확인 완료.** 방법과 증거는 위 D칸에 적었다
- [x] ~~메인 `.env`의 두 값이 낡았다~~ — **2026.09.16 고침.** D를 확인하려다 발견했다
  - `EXPO_PUBLIC_SAVE_REVIEW_TASKS_URL` **추가**. 없으면 `create-learning-history-repository.ts:38`이 서버 저장을 못 켜고, `review-task-store-router.ts:28`이 로그인 사용자한테 예외를 던져 **요청 자체가 안 나간다**
  - `EXPO_PUBLIC_GOOGLE_IOS_CLIENT_ID`를 EAS 값으로 **교체**(`pab7tmss…` → `jc53em95…`). 옛 값이면 로컬 구글 로그인이 `redirect_uri_mismatch`로 막힌다
  - 백업 `.env.backup-20260916-095308` (gitignore). 키 목록 대조 결과 **1개 추가·0개 제거·나머지 그대로**
  - **안드로이드(`GOOGLE_ANDROID_CLIENT_ID`)는 일부러 안 고쳤다.** EAS와 다르지만(`t956itki…` vs `592ipb5d…`), 안드 클라이언트는 **앱 서명 키(SHA-1)에 묶여** 있어 로컬 개발 빌드와 EAS 빌드가 다른 키로 서명되면 클라이언트도 달라야 맞다. 로컬 안드에서 구글 로그인이 막히면 그때 이 줄을 의심할 것 — **검증 안 했다**
  - 홈 방 워크트리는 2026.09.16 기준 사라져 있어 못 고쳤다. 새로 팔 때 `.env`를 메인에서 복사할 것 (gitignore라 워크트리에 안 따라온다)

## 웹(dasida-proto)은 따로 돈다 — 2026.09.19 확인

09.20 첫 학생 통화는 **앱이 아니라 웹**으로 한다. 그래서 위 A~H 칸은 그날 학생 눈에 안 보인다.

- **배포본 = 09.07에 만든 `flow-bundle.js`** (배포본·로컬본 둘 다 134,659바이트). 09.15 앱 수정은 웹에 안 들어갔다 — 그 수정이 "학생 눈에 보이는 변화 0"이라 08.31에 검증한 상태 그대로다
- **09.19에 실제 손글씨 사진으로 끝까지 돌렸다** — 사진 → 분석 → 틀린 자리 지목 → 확인 문제 → 연습 문제 → 오답노트 카드까지 정상. `analyzePhoto` 살아 있다
- ⚠️ **안 고친 결함 4건**: ① 학생 답 말풍선 수식이 흰 글씨+흰 배경이라 안 읽힘 ② 선택지·카드에서 수식 괄호가 안 닫힘(`n(a1+an⁄2`) ③ 문구 "빠져야 안 되는 부분이 빠졌어" ④ **첫 "분석 시작" 클릭이 예외로 죽고 두 번째에 돎** — 통화 중 반응 없으면 한 번 더 누르게 할 것. 상세는 🔧 노션 개발 로그 2026.09.19
- **09.20 22:00 첫 학생 통화 했다 (표본 1).** 결과는 `docs/research/2026-09-20-student-call-1.md`. **화면 ④(채팅: 풀이 방법 확인)에서 막혀 오답노트까지 못 갔다.** 학생이 자기 풀이를 기억 못 했고, AI가 읽은 풀이가 본인 의도와 달랐는데, **선택지에 "이게 아닌데 / 모르겠다"가 없어서** 못 나갔다. 위 결함 4건은 읽기 불편이고 이건 진행 불가 — **결함 ⑤**로 센다. ~~앱 말풍선(`d846d45`)도 같은 전제 위에 있다~~ → **2026.09.21 정정: 틀린 문장이었다.** 그 말풍선(`use-photo-flow.ts:479-512`)에는 **"잘 모르겠어"가 있다**(498줄). 전제가 걸린 건 그 앞 화면들이고, **앱이 더 나쁜 자리가 따로 있다** — 마지막 폴백 `showAllMethods`(`use-photo-flow.ts:244`)에 탈출 버튼이 **아예 없다.** 웹 같은 자리(`app.js:559-567`)에는 "잘 모르겠어"가 있다. 코드 주석이 "서버 호출이 하나 더 붙어서 다음 조각으로 미뤘다"고 적고 있다 — 알고 미룬 자리다. 탈출구를 고르면 그 다음이 뭐냐는 아직 안 정했다

## 모르는 것

- 옛 ⑤가 무엇이었는지 모른다. 저장 방이 ①②④와 ⑥⑦⑧만 말했다
- 실제 학생이 몇 명이나 쓰는지 모른다. Firestore를 조회한 적이 없다
- C의 "55칸"은 코드에서 센 숫자다. 실제 학생 사진에서 몇 %가 붙는지는 안 쟀다. **다만 표본 1은 생겼다 — 2026.09.19에 실제 손글씨 사진(등차수열 합)으로 웹을 돌렸더니 약점 이름표 `수열 계산`이 붙었다.** 1장으로 비율은 못 낸다
- **다른 학생 데이터가 D 때문에 망가졌는지 모른다.** 2026.09.16에 기윤 계정 문서 하나는 눈으로 봤고 정상이었다. 나머지 35명은 안 봤다. 날짜만 적힌 줄이 남아 있으면 `learning-history.ts:1001`의 `.parse()`가 읽을 때 500이 난다
- **Firestore `users`에 한 학생이 문서를 둘 갖고 있다.** `{uid}`에는 profile만, `user:{uid}`에는 학습기록이 들어간다. 동작엔 문제 없어 보이는데 **왜 갈렸는지 안 봤다** (2026.09.16 D방이 지나가다 발견)
- **복습이 실제로 있는 홈을 못 봤다.** 재료가 0건이라 ①②만 눈으로 확인했다. 리스트·D-N 카드는 테스트로만 봤다 — E칸이 풀려야 실물을 본다. 긴 리스트일 때 세로 여백이 어떻게 보이는지도 모른다
- **사진을 찍은 학생이 보는 ②가 어색하다.** "틀린 문제를 찍어서 올리면 여기에 쌓여요"라고 하는데 그 학생은 이미 찍었다. 자기가 한 일이 화면에 안 남는다. 중간 화면("노트 N개 모았어요")을 만들자는 안이 나왔지만 **안 만들었다** — E가 풀리면 이 칸 자체가 거의 사라지므로 그때 다시 보기로 했다
- ~~**E가 얼마나 큰지 모른다.**~~ → **2026.09.21에 쟀다. 맨 위 「그다음 — E칸」 참고 (비테스트 3·테스트 1, 두 판).** 옛 기록: `review-task-store` 쪽을 안 열어봤다 — D방이 보고 있는 영역이라 F방에서 건드리지 않았다
- 태블릿에서 split 레이아웃을 뺀 뒤 여백이 어떻게 보이는지 모른다. 폰과 같은 한 줄 배치를 쓴다
- `.env`·`.env.local`을 worktree로 복사해야 빌드가 된다. gitignore라 안 따라온다
- 로그인한 학생이 실제로 **몇 명인지** 모른다. Authentication에 계정은 36개인데, 그중 몇이 살아 있는지는 안 셌다

## 갱신 규칙

세션 끝에 `npm run log:commit`을 돌린 뒤, **이 파일에서 건드린 칸의 상태와 한 줄을 고친다.** 맨 위 갱신 날짜와 커밋도 같이.

- 칸을 새로 만들면 **다음 글자**를 쓴다. 숫자로 돌아가지 않는다
- 칸이 막히면 🚧로 두고 **무엇이 막는지** 한 줄로 적는다. "진행 중"이라고만 적지 않는다
- 모르는 건 "모르는 것"에 적는다. 아는 척하면 이 파일이 쓸모없어진다
