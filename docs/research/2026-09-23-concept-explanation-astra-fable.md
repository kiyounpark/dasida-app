# 09.23 밤 — 개념 설명을 넣나 · 언제 넣나 — astra·Fable

> 부른 법은 `docs/how-we-decide.md` 그대로. astra = `codex exec` gpt-6-astra(출력 `model:` 줄로 두 번 다 확인), Fable = Agent `model: "fable"`.
> 결론은 `docs/STATUS.md` 「다음 세션 첫 일」 3번. 이 파일은 두 모델 답의 요약 보관소다.

## 계기

09.20 통화 학생 의견 "설명에 개념 설명도 붙여달라. 길어도 가독성 떨어진다고 안 느낀다"(`2026-09-20-student-call-1.md:85`, 말이지 행동은 아님).
09.23 ⑤ 수정 때 "AI를 한 번 더 부르는 설명은 학생 2명 본 뒤 정한다"로 미뤄 뒀다(`web-proto/app.js:609`).
기윤: 친구 2명 연결이 언제 올지 모른다 + 직감으로는 넣어도 된다 → 두 모델에 걸었다.

## 1판 — 넣나 (질문: 지금 / 더 보고 / 안 넣음)

| | 1라운드 | 2라운드 (Fable이 astra 답을 보고) |
|---|---|---|
| astra | **지금 넣는다.** [모르겠어] 뒤, 요청할 때만, 기존 분석 호출에서 같이. 세 덩어리(개념 → 어긴 이유 → 고친 한 단계). 3.1만 토큰 | — |
| Fable | **더 보고 정한다(10.07까지).** 학생은 옛 ⑤에서 멈춰 `why`를 본 적이 없다 — 개념 설명 요청은 고친 화면을 보기 전 의견. GA 맞춤 측정기준(`STATUS.md:79`) 먼저 켜라 | **astra 안으로.** 자기 안은 "아무것도 안 오는 걸 기다리는 안"이었다(친구 미연결, GA 미등록, 21건 중 노트 1건 페이스). 단 셋째 덩어리 "고친 한 단계"는 뺀다 — 프롬프트 "정답 알려주지 마"(`openai-client.ts:445`)와 충돌, 노트에 정답 칸 없음 |

둘이 같았던 것: [모르겠어] 누른 학생에게만 / 새 호출 없이 `analyzePhoto` 한 번에서 / AI 설명은 서버가 수학을 검사 안 한다 → 사람 대조.
기윤: 🔒 넣는다 (Fable 최종안 그대로).

## 2판 — 언제 넣나 (질문: (A) 사용량 로그와 같이 / (B) 1.0.9 뒤 서버+웹만 / (C) 나중)

| | 1라운드 | 2라운드 |
|---|---|---|
| astra | **(A)**, 추가 작업 하루 상한. 넘으면 보류하고 1.0.9 안 막는다. 4~8시간. 4.1만 토큰 | — |
| Fable | **(B)**. 1.0.9엔 어차피 안 보이는데 묶으면 1.0.9만 늦는다. "겹치는 파일 0개" | **(A)로.** 1.0.9는 하루 늦어도 잃는 게 없고 웹은 댓글 통로가 이미 열려 하루가 학생 1명일 수 있다. 겹치는 파일도 틀렸다 |

Claude가 확인해서 Fable에 넘긴 것: 사용량 로그도 `openai-client.ts`를 연다 — `requestPhotoAnalysisFromOpenAI`(`:501-513`)가 `usage`를 안 돌려준다. 자리는 다르다(로그 = 반환부, 개념 = 스키마·프롬프트).

최종(Fable): 로그 먼저 → 바로 이어서 개념. 상한 작업 6시간, 넘으면 개념은 빼고 로그만 배포·1.0.9 그대로.
계정 헤더는 서버에서 선택으로(1.0.8은 `Content-Type`만 보낸다, `analyze-photo-request.ts:109`).
기윤: 사용량 로그 **바로 뒤**에 이어서 (09.23 밤).

## 확인한 것 (Claude가 코드로)

- 1.0.8 앱은 응답을 캐스트만 한다 — `analyze-photo-request.ts:126` `return (await response.json()) as AnalyzePhotoResult;`. 새 키는 그냥 지나간다. 스토어 실기로는 안 돌렸다
- 앱 ⑤는 옛 모양 — `use-photo-flow.ts:341-345` "여기서 틀린 것 같아. 맞아?". 1.0.9 앱엔 개념 설명이 안 보인다
- 두 모델이 댄 숫자(21건 중 노트 1건 `2026-09-23-marketing-pricing-deadline-astra-fable.md:486`, GA 미등록 `STATUS.md:79`, 10.07 `STATUS.md:100`)는 전부 맞았다. 줄 번호는 한두 줄씩 밀린 곳이 있었다

## 토큰

astra 2회 약 7.2만(3.1만 + 4.1만). Fable 4회(두 판 × 2라운드) 약 51만 — Claude 구독 안.
