# 웹으로 온 학생의 "두 번째로 돌아올 자리" — astra·Fable (2026.09.23 저녁)

> 결정 규칙: `docs/how-we-decide.md` — 둘에 같은 질문, 갈리면 Fable이 astra 답을 보고 최종.
> 이 파일은 원문 보관용이다. 요약·결정은 `docs/STATUS.md` 맨 위와 「🧭 순서」.

## 왜 물었나

댓글 통로로 오는 학생은 **웹**으로 온다. E(사진 노트 → 복습 과제)는 **앱**에만 생긴다.
웹엔 로그인·저장·복습·방문자 식별이 없다. 판정선은 전부 "다른 날 두 번째로 온 학생"이다.

## 결론 (Fable 최종, 기윤 OK)

1. **E는 여전히 다음 개발.** 11.18·3월 판정은 앱 기준(반복 사진·반복 복습)이라 E + 로그 없이는 못 센다
2. **10.07은 손 대장으로 센다 (0파일).** 두 번째 풀이는 오르비로 기윤에게 온다. 대장 칸은 astra 것 —
   오르비 계정 · 댓글 URL · 첫 풀이 날짜(공개 글이면 원문 명시) · 둘째 풀이 날짜(KST 다른 날). 재시도·같은 사진·기윤 대리 실행 제외, AI 실패는 따로
3. **댓글 통로는 E를 기다리지 않는다 — 지금 나란히.** (오전 합의 "셋 끝나면 개발 멈추고 마케팅"을 바꿈)
4. **예약급 — 로그 필드 둘:** `accountKey`(앱) + `participantId`(웹 링크 코드). 섞지 않는다 (Fable이 자기 "같은 헤더" 안을 버리고 astra 안을 받음)
5. **웹 엔딩:** `app.js:807` "아직 앱엔 없어" 거짓 문장·`:821` 수요 조사 버튼 제거 → "다음에 막힌 풀이도 보내줘". 링크 복사 버튼 안 만듦. 스토어 버튼은 1.0.8 라이브 확인 뒤

Claude 확인: 마케팅 문서 `:214`·`:216`, 09.23 문서 `:993`, `app.js:807`·`:809`·`:821`, `learning-history-auth.ts:6` — 전부 인용대로 있다.
토큰: astra 54,202 · Fable 약 25만(두 번).

---

## astra 원문 (gpt-6-astra)

1. **C＋개인별 재제출 링크를 고릅니다. E는 다음 일에서 뺍니다.** 순서는 **웹 재방문 안내·계측 → 댓글 통로 → E·계정별 사용량 로그**로 변경을 권합니다.
   10.07에 필요한 자리는 “다음 풀이를 보내는 곳”입니다. 사진 재제출과 복습 재방문은 다른 지표이므로, 이번 결과를 복습 수요의 증거로 세지는 않습니다.

2. **근거:** 기존 결정도 “요청한 학생에게 웹 링크 → 다음에 틀린 것 하나 더”입니다. 개인 링크는 이 통로를 유지합니다. [마케팅 원문:214](/Users/baggiyun/dev/dasida-app/docs/research/2026-09-22-marketing-astra-fable.md:214)
   웹 엔딩은 현재 **설치 버튼이 아니라 수요 조사 버튼**이고, 다른 문제 버튼은 새로고침입니다. [app.js:810](/Users/baggiyun/dev/dasida-app/web-proto/app.js:810)
   E가 웹 학생에게 닿지 않는다는 사실은 기존 순서 문서에도 명시돼 있습니다. [STATUS.md:72](/Users/baggiyun/dev/dasida-app/docs/STATUS.md:72)
   설치 부담의 **전환율 실측은 없습니다**. 자연 유입 미측정 기록과 09.07 심사 제출 기록만 확인했습니다. 설치가 이탈을 늘린다는 것은 **짐작**이며, 현재 스토어판 기능도 제출 기록만으로 확정하지 않습니다. [마케팅:78](/Users/baggiyun/dev/dasida-app/docs/research/2026-09-22-marketing-astra-fable.md:78) · [PROGRESS:3738](/Users/baggiyun/dev/dasida-app/docs/PROGRESS.md:3738)

3. **10.07 최소판:** 요청한 학생마다 무작위 참여 코드를 발급해 개인 웹 링크를 주고, 운영대장에 **코드↔오르비 계정·댓글 URL·첫 풀이 날짜**를 연결합니다.
   서버에는 `participantId / submissionId / receivedAt / 분석 결과 상태 / QA 여부`를 남깁니다. 지금 요청·로그에는 학생 식별자가 없습니다. [analyze-photo.ts:24](/Users/baggiyun/dev/dasida-app/functions/src/analyze-photo.ts:24) · [86](/Users/baggiyun/dev/dasida-app/functions/src/analyze-photo.ts:86)
   집계는 **댓글 받은 동일 학생이 KST 기준 다른 날 새 풀이를 제출했는가**입니다. 재시도·같은 사진 재전송·개발자 대리 실행은 제외하고, AI 실패는 제출 사실과 따로 셉니다.
   원래 공개 글 사진을 첫 풀이로 삼으면 원문·날짜를 대장에 명시합니다. 이후 제출과 같은 학생인지는 오르비 대화로 확인합니다. **코드 일치만으로 사람을 확정하지 않습니다.**
   **예상 5파일·6~10시간:** 웹 `app.js`, 서버 `analyze-photo.ts`, 기록 모듈·테스트 각 1개, 집계 절차 문서 1개. 배포·날짜 경계·중복·QA 제외 검증 포함한 **견적**입니다.

4. **되돌리기 비싼 것:** 참여자와 브라우저를 같은 식별자로 잡는 것입니다. `participantId`를 고정하고 브라우저·향후 인증 계정은 별도 연결하세요.
   그래야 기기를 바꾸거나 로그인해도 첫 제출과 두 번째 제출을 이어 셉니다. 참여 코드를 향후 노트 조회 권한으로 재사용하지는 않습니다.

5. **학생 화면:** 엔딩을 **“다음에 막힌 풀이도 여기로 보내줘”＋[내 제출 링크 복사]＋[다른 문제 올리기]**로 바꿉니다.
   링크를 다시 열면 바로 사진 제출 화면입니다. 기존 오르비 대화에서도 같은 링크를 다시 찾을 수 있습니다.
   저장 안 되는 노트는 기존처럼 캡처 안내를 유지하고, 현재 엔딩의 앱 기능 수요 조사·복습 약속을 재제출 안내로 교체합니다. [app.js:804](/Users/baggiyun/dev/dasida-app/web-proto/app.js:804)

---

## Fable 1차 원문

**1. 안 — C(손으로 센다) + 웹 엔딩 문구 한 줄 수정. E는 여전히 다음 개발이지만, 댓글 통로를 E 뒤에 둘 이유는 없다 — 지금 열어도 된다.**

**2. 근거**
- 10.07 판정선의 "두 번째 풀이"는 웹이 아니라 기윤에게 오르비로 온다. `docs/research/2026-09-22-marketing-astra-fable.md:214` "그 학생에겐 '다음에 틀린 거 하나 더 보내줘'", `:216` "두 번째 풀이를 보낸 학생 1명". 09.23 문서 `:993`이 못박았다 — "댓글로 개발자에게 두 번째 풀이를 보낸 것과 앱에 두 번째 사진을 올린 것은 따로 셉니다." 즉 10.07은 코드 없이 셀 수 있다. 웹 식별자는 10.07엔 필요 없다.
- STATUS가 E를 앞세운 이유("E 없으면 돌아올 자리가 없고 로그 없으면 못 센다")는 학생이 앱에 온다는 전제인데, 같은 절이 스스로 "댓글로 온 학생은 웹으로 온다"고 적었다. 전제가 어긋난 채 순서가 잡혔다.
- 웹 엔딩이 학생한테 거짓말을 한다: `web-proto/app.js:807` "사진으로 노트 만드는 건 아직 앱엔 없어 — 지금 만드는 중이야", `:809-811` 스토어 버튼 제거. 그런데 STATUS A칸 ✅, `docs/PROGRESS.md:3738` 09.07 1.0.8 심사 제출. 1.0.8이 스토어에 실제 올라갔는지는 저장소에서 확인 못 했다.
- 앱 설치 무게: `PROGRESS.md:3738` "Play Console 설치 수 0"(09.07), 09.23 문서 `:185` "스토어 유입은 한 번도 안 재봤다", `:383` 앱은 첫 값까지 화면 다섯. 설치 전환율 데이터는 0건 — 그래서 B(웹 저장·로그인)도 A(앱 강제)도 지금 지을 근거가 없다.
- 웹·앱·서버 셋 다 `analyzePhoto`에 식별자를 안 보낸다: `app.js:294-298`, `features/photo/flow/analyze-photo-request.ts:107-111`, `functions/src/analyze-photo.ts:25-30`.

**3. 10.07 최소판 — 파일 0개, 0시간.** 표 한 장. 원하면 1시간짜리 덤: 링크에 `?s=코드` → `analyzePhoto` body에 선택 필드 → `logger.info`에 한 줄.

**4. 되돌리기 비싼 것 — 로그 키의 정체.** 앱은 `x-dasida-account-key`, 웹은 다른 이름이면 둘을 영영 못 잇는다. 웹 코드도 같은 헤더·같은 필드에. (→ 2차에서 버림)

**5. 학생 화면 변화 — 한 줄.** "아직 앱엔 없어"를 고치고 스토어 버튼 복구. 단 1.0.8 라이브 확인이 먼저.

---

## Fable 최종 (astra 답을 보고)

**1. 순서 — E는 그대로 다음 개발. 댓글 통로는 E를 기다리지 않는다(병렬).**
10.07의 "두 번째 풀이"는 오르비로 기윤에게 온다 — 웹 자리가 없어도 판정은 성립하니 astra의 "웹 자리 먼저"는 근거가 약하다. 반면 11.18·3월 판정표는 앱 기준 반복 사진·반복 복습이라 E+계정 로그 없이는 아예 못 세고, 그 둘은 아직 시작도 안 했다.

**2. 10.07 최소판 — 손 대장 한 장, 0파일. astra의 집계 규칙만 가져온다.**
astra 스스로 "코드 일치만으로 사람 확정 안 함, 오르비 대화로 확인"이라 했으니 6~10시간짜리 서버 기록은 10.07엔 남는 게 없다.

**3. 되돌리기 비싼 것 — astra가 맞다. 내 "같은 헤더" 안은 버린다.**
`x-dasida-account-key`는 `learning-history-auth.ts:6`에서 계정 조회 키로 쓰이는 헤더라, 웹 참여 코드를 거기 넣으면 나중에 인증 붙는 순간 코드가 계정으로 둔갑한다. 로그 필드를 둘로 고정한다 — `accountKey`(앱, 있으면) + `participantId`(웹 링크 코드, 있으면). 잇는 건 대장에서.

**4. 학생 화면 — `:807` 거짓 문장·`:821` 수요 조사 버튼을 없애고, 엔딩 말을 "다음에 막힌 풀이도 보내줘"로. 링크 복사 버튼은 안 만든다.**
[내 제출 링크 복사]는 2번 시스템이 있어야 뜻이 있는 버튼이라 손 대장 안에선 빈 버튼이다. 스토어 버튼은 1.0.8이 스토어에 실제 떠 있는지 확인한 뒤에만 켠다.

**다음 개발 한 줄:** `app.js` 엔딩 두 줄(30분) → E(`source:'photo'` 양쪽) → `analyzePhoto` 로그에 `accountKey`·`participantId` 두 필드·`usage`.
**댓글 통로 여는 시점:** 지금 — 오르비에서 풀이 사진 글이 하루 몇 편인지 세는 1일차 컷부터, E와 나란히.
