/**
 * 개발 빌드에서는 실제 계측 서버(GA4·PostHog)로 보내지 않는다.
 *
 * DAU가 한 자릿수라 시뮬레이터에서 몇 번 눌러본 게 지표를 통째로 뒤집는데,
 * 두 서비스 모두 지난 데이터를 못 지운다.
 *
 * 2026.08.13에 web-proto에서 같은 자리를 잡았지만(로컬 미발신 + `?qa=1` 제외)
 * 앱에는 그 분리가 없었다. 08.27에 사진 계측을 붙이다 발견 —
 * 하필 1.0.8이 재려는 숫자를 우리 손이 먼저 채울 뻔했다.
 *
 * `__DEV__`는 빌드 타임 상수라 출시·TestFlight 빌드에서는 항상 false다.
 * 웹의 `?qa=` 스위치와 달리 "한 번 새면 진짜 학생이 영영 빠지는" 링크가 없다.
 */
export function analyticsEnabled(): boolean {
  return !__DEV__;
}
